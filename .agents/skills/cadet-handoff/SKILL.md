---
name: cadet-handoff
description: Summarize the current session's work and record what must happen next, so a new chat can continue without re-discovery. Use when wrapping up, handing off, or before starting a fresh chat.
---

## Primary Context

Read `.cadet/agent/core/cadet-agent.md` for the global directive, then read `.cadet/agent/core/skills/Handoff.md` as the primary instruction context — in full, including the `<role>` block (it defines the persona for this turn). Follow every step in that file: gate check, process, required inputs, expected outputs, and completion criteria.
